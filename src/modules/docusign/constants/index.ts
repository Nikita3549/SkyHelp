export const DEFAULT_ENVELOPE_STATUS = 'sent';

export const DEFAULT_AUTHENTICATION_METHOD = 'none';

export const DEFAULT_TOKEN_JWT_PAYLOAD = 'signature impersonation';

export const INVALID_CLAIM_ID = 'Invalid claim id';
