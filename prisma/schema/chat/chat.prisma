model Chat {
  id        String   @id @default(uuid())
  type      ChatType @default(DIRECT)
  createdAt DateTime @default(now()) @map("created_at")

  messages Message[]
  members  ChatMember[]

  @@map("chats")
}
