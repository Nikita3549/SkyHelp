name: Deploy

on:
  workflow_dispatch:
    inputs:
      environment:
        description: "Select environment to deploy"
        required: true
        default: "prod"
        type: choice
        options:
          - prod
          - both
  push:
    branches:
      - master

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Validate .env
        uses: appleboy/ssh-action@v0.1.10
        with:
          host: ${{ secrets.SERVER_HOST }}
          username: ${{ secrets.SERVER_USER }}
          key: ${{ secrets.SERVER_SSH_KEY }}
          port: ${{ secrets.SERVER_SSH_PORT }}
          script: |
            cd ~/SkyHelp || { echo "❌ PROD directory not found"; exit 1; }
            bash validate-env.sh
            
            cd ~/SkyHelpDev || { echo "❌ Dev directory not found"; exit 1; }
            bash validate-env.sh

      - name: Save last commit on PROD
        uses: appleboy/ssh-action@v0.1.10
        if: github.event_name == 'workflow_dispatch' && (github.event.inputs.environment == 'prod' || github.event.inputs.environment == 'both')
        with:
          host: ${{ secrets.SERVER_HOST }}
          username: ${{ secrets.SERVER_USER }}
          key: ${{ secrets.SERVER_SSH_KEY }}
          port: ${{ secrets.SERVER_SSH_PORT }}
          script: |
            cd ~/SkyHelp || { echo "❌ PROD directory not found"; exit 1; }
            git rev-parse HEAD > .last_commit

      - name: Deploy PROD
        uses: appleboy/ssh-action@v0.1.10
        if: github.event_name == 'workflow_dispatch' && (github.event.inputs.environment == 'prod' || github.event.inputs.environment == 'both')
        with:
          host: ${{ secrets.SERVER_HOST }}
          username: ${{ secrets.SERVER_USER }}
          key: ${{ secrets.SERVER_SSH_KEY }}
          port: ${{ secrets.SERVER_SSH_PORT }}
          script: |
            cd ~/SkyHelp || { echo "❌ PROD directory not found"; exit 1; }
            last_commit=$(cat .last_commit)

            echo "✅ Pulling latest changes (PROD)"
            git pull origin master || { echo "❌ Git pull failed"; exit 1; }
            source .env

            echo "🔧 Building and restarting PROD containers"
            docker exec -i db-static psql \
              -U "$DATABASE_STATIC_USER" \
              -d "$DATABASE_STATIC_DBNAME" \
              -f /docker-entrypoint-initdb.d/update.sql

            if ! docker compose --profile api up --build -d --remove-orphans; then
              echo "❌ Docker Compose failed — rolling back"
              git reset --hard $last_commit
              docker compose --profile api up -d --remove-orphans || { echo "❌ Rollback failed"; exit 1; }
              exit 1
            fi

            echo "✅ PROD Deployment complete"

      - name: Deploy DEV
        uses: appleboy/ssh-action@v0.1.10
        with:
          host: ${{ secrets.SERVER_HOST }}
          username: ${{ secrets.SERVER_USER }}
          key: ${{ secrets.SERVER_SSH_KEY }}
          port: ${{ secrets.SERVER_SSH_PORT }}
          script: |
            cd ~/SkyHelpDev || { echo "❌ DEV directory not found"; exit 1; }

            echo "✅ Pulling latest changes (DEV)"
            git pull origin master || { echo "❌ Git pull failed"; exit 1; }

            echo "🔧 Building and restarting DEV containers"
            docker compose -f docker-compose.yml -f docker-compose.dev.yml -p dev --profile api up --build -d --remove-orphans


            echo "✅ DEV Deployment complete"